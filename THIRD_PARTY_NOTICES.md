# Third-party notices

This application bundles or depends on open-source and commercial packages (for example via `pnpm` and `package.json`). Each dependency remains under **its own license**; this file does not relicense them.

To inspect dependency metadata:

- `package.json` and `pnpm-lock.yaml`
- Run `pnpm licenses list` (pnpm 9+) or your organization’s approved SBOM tooling

Notices for large vendored assets (if any) should be added here as they are introduced.
